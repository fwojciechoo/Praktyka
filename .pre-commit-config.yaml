repos:
    - repo: local
      hooks:
        - id: check-commit-message
          name: Check commit message
          entry: python scripts/check-commit-message.py
          language: system
          stages:
            - pre-commit
